{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/enterprise.md/schemas/initiatives.schema.json",
  "title": "initiatives.yml",
  "type": "object",
  "required": [
    "spec_name",
    "spec_version",
    "initiatives"
  ],
  "properties": {
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Deprecated field. A bare version no longer satisfies the header contract; emit spec_name and spec_version. Retained only so pre-1.x files still parse."
    },
    "initiatives": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/initiative"
      }
    },
    "spec_name": {
      "type": "string",
      "const": "initiatives"
    },
    "spec_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Full MAJOR.MINOR.PATCH semantic version."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "spec_version": {"pattern": "^1\\.0\\."}
        },
        "required": ["spec_version"]
      },
      "then": {
        "properties": {
          "initiatives": {
            "items": {
              "not": {
                "anyOf": [
                  {"required": ["standards_domain_id"]},
                  {"required": ["pattern_index_ref"]}
                ]
              }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "approved",
        "ready",
        "in_progress",
        "paused",
        "completed",
        "archived",
        "deprecated",
        "inactive"
      ]
    },
    "initiative": {
      "type": "object",
      "required": [
        "initiative_id",
        "solution_repo_url",
        "solution_entrypoint",
        "solution_git_ref",
        "status"
      ],
      "properties": {
        "initiative_id": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "name": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "solution_repo_url": {
          "type": "string",
          "format": "uri",
          "minLength": 1
        },
        "solution_entrypoint": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "solution_git_ref": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "status": {
          "$ref": "#/$defs/status"
        },
        "owner": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "standards_domain_id": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Optional governed standards-provider selection. Canonical from initiatives 1.1.0."
        },
        "pattern_index_ref": {
          "$ref": "#/$defs/safeRelativePath",
          "description": "Optional pattern-index override for the selected standards provider."
        },
        "metadata": {
          "type": "object"
        }
      },
      "additionalProperties": true
    },
    "safeRelativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?![A-Za-z]:)(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"
    }
  },
  "additionalProperties": true
}
