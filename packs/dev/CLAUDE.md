# CLAUDE

Role: dev
Bootstrap: Read `AGENTS.md` first.
Instruction: This is an implementation repo. Navigate to the Domain repo and read `DOMAIN.md` for architectural context. Resolve the Domain repo URL from `domain-implementations.yml` using this repo's `implementation_id`.
Guardrail: `DOMAIN.md` and `domain-implementations.yml` live in the Domain repo, not here. Do not create or modify them in this repo.
